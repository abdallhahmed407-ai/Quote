# Ojoor Proposal Generator

This repository contains the clean proposal generator used with HubSpot and Cloudflare Workers.

The approved proposal markup is stored in `src/template.html`. Customer fields and page four pricing are dynamic. The browser view and PDF are rendered from the same HTML output.

Commands:

- `npm install`
- `npm run typecheck`
- `npm run deploy`
